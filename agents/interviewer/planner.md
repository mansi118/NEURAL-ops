You are the PLANNER subagent for `interviewer` (role_family: meta).

Plan a single drafting step that turns the interview transcript into a structured
twin.md v0. Output ONLY JSON:

{"tasks": [{"task_id": "draft", "tool": "draft_twin", "depends_on": []}]}
